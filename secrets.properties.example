# Copy this file to `secrets.properties` (which is gitignored) and fill in your keys.
# NEVER commit real keys. `secrets.properties` is read at build time and exposed to the
# Android app via BuildConfig (see androidApp/build.gradle.kts). For iOS/web, see
# docs/live-captions-plan.md (Phases 3/4).
#
# Get keys:
#   Deepgram: https://console.deepgram.com  (free $200 credit)
#   Soniox:   https://console.soniox.com
DEEPGRAM_API_KEY=
SONIOX_API_KEY=
